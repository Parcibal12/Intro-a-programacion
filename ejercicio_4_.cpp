#include <iostream>

using namespace std;

int main() {
    int x, y, z = 1;
    cout << "Coloca dos números enteros: ";
    cin >> x >> y; 
    cout << "Respuesta: " << x + y + z << endl; 
    return 0; 
}
