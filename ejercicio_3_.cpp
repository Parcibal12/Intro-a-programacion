#include <iostream>

using namespace std;

int main() {
    int anchura, altura; 
    cout << "Coloque la altura y anchura de su rectangulo: "; 
    cin >> anchura >> altura; 
    cout << "El area del rectangulo es: " << anchura * altura << endl; 
    return 0; 
}
